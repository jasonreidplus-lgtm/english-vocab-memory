node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
npm-debug.log*

# vocab 重建临时文件
tmp_dict/
*.zip
KaoYan_*.json

# 词典构建临时源(ECDICT，约 63MB，不入库；dict.json 产物才入库)
data-src/_ecdict.csv

# 富字段生成的临时分块(产物并入 vocab.json 后即无用)
data-src/rich-todo/
data-src/rich-done/

scripts/targets/
scripts/out/

# 由 vocab.json 生成的懒加载拆分文件(prebuild/predev 自动生成)
public/data/vocab-index.json
public/data/groups/
